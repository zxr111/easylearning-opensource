<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.easylearning.elweb.dao.UserDAO">

    <insert id="create" parameterType="User">
        INSERT INTO `user`(`userid`, `username`, `phone`, `password`)
        VALUES (#{userid}, #{username}, #{phone}, #{password})
    </insert>

    <update id="update" parameterType="">
        UPDATE `user` SET
    </update>

    <delete id="remove" parameterType="">
    </delete>

    <select id="findById" parameterType="Object" resultType="User">

    </select>

</mapper>